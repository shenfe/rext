{
    "scripts": {
        "build": "./node_modules/.bin/rollup -c ./rollup.config.js",
        "dev": "./node_modules/.bin/rollup -w -c ./rollup.config.js",
        "test": "echo 'no test now'"
    },
    "devDependencies": {
        "rollup": "^0.49.2",
        "rollup-plugin-uglify": "^2.0.1",
        "uglify-js": "^3.0.28"
    }
}
